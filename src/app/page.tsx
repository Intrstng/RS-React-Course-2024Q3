import React from 'react';
import App from '../myApp/App';

export const metadata = {
  title: 'RS School Next.js',
};

const Home = () => {
  return (
    <div>
      <App />
    </div>
  );
};

export default Home;
