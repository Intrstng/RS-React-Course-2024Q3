/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: 'export', // is used for Route API
  distDir: './dist',
  // basePath: process.env.NEXT_PUBLIC_BASE_PATH,
};

export default nextConfig;
